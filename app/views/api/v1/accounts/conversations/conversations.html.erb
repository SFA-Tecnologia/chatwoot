<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <title>Example PDF</title>
  <style>
    /* Add your CSS styles here */
    body {
      font-family: "Helvetica Neue", Helvetica, Arial, sans-serif;
      padding: 20px;
    }

    .header {
      text-align: center;
      margin-bottom: 40px;
    }

    .content {
      margin: 0 auto;
      width: 80%;
    }

    table {
      width: 100%;
      border-collapse: collapse;
    }

    table, th, td {
      border: 1px solid black;
    }

    th, td {
      padding: 8px;
      text-align: left;
    }
  </style>
</head>
<body>
<header>
  <h1>Teresa 360º</h1>
  <% if @assigned_agent_name %>
    <p>Agente: <%= @assigned_agent_name %></p>
  <% end %>
  <% if @date_range %>
    <p>Data: <%= @date_range %></p>
  <% end %>
</header>


<div class="content">
  <table>
    <tr>
      <th>Nome do Usuário</th>
      <th>Protocolo</th>
      <th>Data de Chegada</th>
      <th>Data de Atendimento</th>
      <th>Status</th>
    </tr>
    <% # Assuming @items is defined in your controller %>
    <% @conversations.each do |conversation| %>
      <tr>
        <td><%= conversation.contact.name %></td>
        <td><%= conversation.additional_attributes['protocolo'] || '---' %></td>
        <td><%= conversation.created_at ? conversation.created_at.strftime('%d/%m/%Y %H:%M') : '---' %></td>
        <td><%= conversation.first_reply_created_at ? conversation.first_reply_created_at.strftime('%d/%m/%Y %H:%M') : '---' %></td>
        <td><%= I18n.t("chat_list.chat_status_filter_items.#{conversation.status}.text") %></td>
      </tr>
    <% end %>
  </table>
</div>
</body>
</html>
